import { View, Text } from "react-native";

export default function LoginCallback() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Login callback</Text>
    </View>
  );
}